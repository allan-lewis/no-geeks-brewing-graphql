type Query {
    batches(batchDate: Float): [Batch]
}

type Batch {
    batchId: ID!
    batchName: String
    batchNumber: Float
    batchDate: Float
    batchStatus: String
    batchStyle: String
}
